# Firebird Command
- install dependencies
`npm install`
- compile source
`npm run build`
- start server
`npm start`
- build docs with documentation.js
`documentation build src/** -f html -o docs --shallow`
